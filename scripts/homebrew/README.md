Install quell locally with Homebrew (no tap required):

    brew install --formula scripts/homebrew/quell.rb

Replace the `PLACEHOLDER_SHA256_*` values in `quell.rb` with real checksums before publishing to a tap.
